# Server
PORT=3000
NODE_ENV=development

# CORS: in production, set to the exact origin(s) of your frontend (comma-separated).
# If unset in production, no cross-origin requests are allowed (same-origin only).
# Example: https://app.example.com,https://diem.example.com
# CORS_ORIGINS=

# Database
DATABASE_PATH=./data/decan.db
# Optional: encrypt DB at rest (SQLite3 Multiple Ciphers). Set before first run for new DBs; for existing plain DB, see backend README.
# DB_ENCRYPTION_KEY=your_32_char_or_longer_secret

# Blockchain (Base Sepolia for testnet)
RPC_URL=https://sepolia.base.org
CONTRACT_ADDRESS=0x648877Fcc28536e37BDb10c702156d8C2F0d0159
PRIVATE_KEY=your_private_key_here

# Venice API
VENICE_API_KEY=your_venice_api_key_here
VENICE_BASE_URL=https://api.venice.ai

# Platform (unused in MVP, owner is contract deployer)
PLATFORM_FEE_BASIS_POINTS=100

# Optional: require Authorization: Bearer <secret> or X-API-Key: <secret> for GET/DELETE /api/webhooks/subscriptions
# WEBHOOK_ADMIN_SECRET=

# USDC (testnet: mintable token; mainnet will use a different address)
USDC_ADDRESS=0x6Ac3aB54Dc5019A2e57eCcb214337FF5bbD52897
